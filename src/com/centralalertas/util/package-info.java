/**
 * Utilitarios de apoio, sem regra de negocio: conexao JDBC (Conexao), JSON com Gson
 * (Json) e helpers de HTTP (Http: ler o corpo, escrever JSON, CORS). Usados pelas
 * demais camadas.
 */
package com.centralalertas.util;
